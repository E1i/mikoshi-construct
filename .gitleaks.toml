[extend]
useDefault = true

[allowlist]
description = "a construct manifest stores sha256 hashes of baseline files, including .claude/rules/secrets.md; frozen manifests live under tests/fixtures too"
paths = [ '''(^|/)construct\.json$''' ]
