{
  "name": "healthy",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "typecheck": "node --version",
    "test": "vitest run",
    "quality": "pnpm lint && pnpm typecheck && pnpm test",
    "precommit": "pnpm run quality"
  },
  "devDependencies": {
    "lefthook": "^1.14.0"
  }
}
