{
  "name": "quality-not-in-ci",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsup",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "quality": "pnpm composition:check && pnpm lint && pnpm typecheck && pnpm test"
  }
}
