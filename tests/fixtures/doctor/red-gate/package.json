{
  "name": "red-gate",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "typecheck": "node --version",
    "test": "node --version",
    "quality": "pnpm lint && pnpm typecheck && pnpm test"
  }
}
