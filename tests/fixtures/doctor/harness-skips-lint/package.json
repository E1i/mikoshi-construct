{
  "name": "harness-skips-lint",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "typecheck": "node --version",
    "test": "vitest run",
    "quality": "pnpm composition:check && pnpm typecheck && pnpm test",
    "precommit": "pnpm run quality"
  },
  "devDependencies": {
    "lefthook": "^1.14.0"
  }
}
