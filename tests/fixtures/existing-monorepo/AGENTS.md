# example-monorepo

A cashback service: `apps/api` serves the public HTTP API, `packages/shared` holds the contract types.

## Commands

- `pnpm dev` — run the API locally
- `pnpm run quality` — the gate

## Architecture

See [docs/architecture/overview.md](docs/architecture/overview.md).
